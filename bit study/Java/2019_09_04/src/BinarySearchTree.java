/*import java.util.*;

public class BinarySearchTree {
    public static class Node{
        int key;
        int val;

        Node left;
        Node right;
    }
    private  Node root = null;

    public int getOrfault(int key, int defaultValue) {
        Node cur = root;
        while(true){
            if(root == null){
                return -1;
            }
            if(){

            }
            List<String> list = new LinkedList<>();
            if (defaultValue == key) {
                list.add()
            }
            return -1;
        }
    }
    public Set<Integer> keySet(){
        Set<Integer> restult = new HashSet<>();
        Queue<Node > queue = new LinkedList<>();
        if(root == null){
            return restult;

        }
        while(!queue.isEmpty()){

        }
    }
}
*/
